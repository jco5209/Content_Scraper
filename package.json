{
  "name": "package.json",
  "version": "1.0.0",
  "description": "xray scraper - dependencies include: npm xray && ( an npm csv creator perhaps )",
  "main": "xray-scrape.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^3.11.1",
    "eslint-config-google": "^0.7.1"
  }
}
